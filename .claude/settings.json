{
  "autoApprove": false,
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -q 'git commit' && jq -n '{hookSpecificOutput: {permissionDecision: \"deny\", permissionDecisionReason: \"git commit blocked — say зроби коміт first\"}}' && exit 0 || exit 0"
          }
        ]
      }
    ]
  }
}
