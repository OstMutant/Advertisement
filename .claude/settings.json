{
  "autoApprove": false,
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git commit' && echo '⛔ git commit blocked — requires explicit user approval' >&2 && exit 2 || exit 0"
          }
        ]
      }
    ]
  }
}
