package org.ost.platform.taxon.model;

/**
 * Closed set of taxon classifier types. Adding a new value is a release-level change:
 * it requires UI integration, audit translations, and seed entries.
 */
public enum TaxonType {
    CATEGORY
}
