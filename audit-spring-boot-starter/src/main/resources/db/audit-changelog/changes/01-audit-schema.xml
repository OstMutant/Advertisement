<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="01-audit-schema" author="ostap">
        <validCheckSum>ANY</validCheckSum>

        <!-- ── audit_log ───────────────────────────────────────────────────── -->
        <createTable tableName="audit_log"
                      remarks="Immutable write-only table (never updated, only inserted). snapshot_data format varies by action type: CREATED/RESTORED -> {snapshot: {...}}, UPDATED -> {before: {...}, after: {...}}, DELETED -> {snapshot: {...}}. JSONB allows flexible schema for different entity types. Point-in-time recovery via AuditPort.getSnapshotContent(snapshotId, entityType).">
            <column name="id" type="BIGSERIAL" remarks="Auto-increment">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="entity_type" type="VARCHAR(50)" remarks="Type of audited entity (from EntityType enum)">
                <constraints nullable="false"/>
            </column>
            <column name="entity_id" type="BIGINT" remarks="ID of audited entity">
                <constraints nullable="false"/>
            </column>
            <column name="action_type" type="VARCHAR(50)" remarks="Action (CREATED, UPDATED, DELETED, RESTORED)">
                <constraints nullable="false"/>
            </column>
            <column name="snapshot_data" type="JSONB" remarks="Before/after snapshot: {before: {...}, after: {...}} or {snapshot: {...}}"/>
            <column name="actor_id" type="BIGINT" remarks="User ID who made the change. References user_information(id), no FK">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="NOW()" remarks="Timestamp of change">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex indexName="idx_audit_entity" tableName="audit_log">
            <column name="entity_type"/>
            <column name="entity_id"/>
            <column name="created_at" descending="true"/>
        </createIndex>

        <createIndex indexName="idx_audit_changed_by" tableName="audit_log">
            <column name="actor_id"/>
            <column name="created_at" descending="true"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
