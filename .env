# Single source of truth for values shared between scripts/infra/docker-compose.db.yml and
# test-support's Testcontainers-based repository tests (AbstractPostgresIntegrationTest).
# Docker Compose reads this file automatically from the current working directory.
# See features/issues/improvement-044-shared-env-config-consolidation.md.
POSTGRES_IMAGE=postgres:15-alpine
