# ── Header ──────────────────────────────────────────────────────────────────
# Description: Default flag values, read as a baseline by an external caller; any CLI flag
#   (--unit/--no-unit, --integration/--no-integration) passed explicitly overrides the
#   corresponding value for that one invocation only.
# Usage: not invoked directly -- read by an external caller as its own default flag values.
# Env: None.
# ────────────────────────────────────────────────────────────────────────────
unit=true
integration=true
